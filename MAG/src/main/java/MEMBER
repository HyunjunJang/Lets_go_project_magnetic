CREATE TABLE MEMBER (
    MEMBER_ID VARCHAR2(30) NOT NULL,
    NAME VARCHAR2(30),
    PW VARCHAR2(30),
    NICK VARCHAR2(30),
    AGE NUMBER(10),
    GENDER CHAR(1) DEFAULT 'M',
    TYPE VARCHAR2(30),
    CONSTRAINT MEMBER_PK PRIMARY KEY (MEMBER_ID)
); 